# Changelog

## 2026-04-19

- Added a WSL-first shareable package for private publication
- Added `codex-image-batch.sh` as the portable runner
- Added sample JSON files for generation and editing
- Added beginner-focused `README`
- Added `QUICKSTART` for the shortest working path
- Added `EXTERNAL-GUIDE` as an external-facing summary of what was validated and how to use the package safely
- Added `--doctor` environment diagnostics
- Added `--list-presets` to print built-in aspect/style presets
- Added execution confirmation before real runs
- Added support for Windows path input such as `C:\...` and `\\wsl.localhost\...`
- Added safer relative-path reporting in summaries and log references when possible
- Added beginner guardrails for common mistakes such as folder-vs-file confusion and invalid JSON errors
